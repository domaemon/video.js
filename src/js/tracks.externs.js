/**
 * @fileoverview Externs for videojs.TextTrack. Externs are functions that the
 * compiler shouldn't obfuscate.
 */

/**
 * @constructor
 * @extends {videojs.Component}
 */
videojs.TextTrack = function(){};

/**
 * Text tracks
 */
videojs.TextTrack.prototype.parseCues = function(){};
